# 故障排除指南

## 🚨 常见问题及解决方案

### 1. 管理后台按钮无响应

**问题描述**: 点击"添加成员"或"添加分组"按钮没有任何反应。

**解决方案**:
1. **重启开发服务器**:
   ```bash
   # 停止服务器后重新启动
   npm run dev
   ```

2. **清除浏览器缓存**:
   - 按 `Ctrl+Shift+R` (Windows) 或 `Cmd+Shift+R` (Mac)
   - 或者在开发者工具中禁用缓存

3. **检查浏览器控制台**:
   - 按 `F12` 打开开发者工具
   - 查看 Console 标签页是否有 JavaScript 错误

4. **验证修复状态**:
   ```bash
   node test-admin-buttons.js
   ```

### 2. 路径别名错误 "Failed to resolve import '@/services/api'"

**问题描述**: Vue组件无法解析 `@/` 开头的路径别名。

**解决方案**:
1. 确保 Vite 配置正确（已修复）
2. 重新安装前端依赖
3. 重启开发服务器

```bash
# 重新安装前端依赖
cd client
rm -rf node_modules package-lock.json
npm install

# 重启开发服务器
npm run dev
```

### 3. 端口被占用

**问题描述**: 端口3000或5173被其他程序占用。

**解决方案**:
```bash
# 查看端口占用情况
netstat -ano | findstr :3000
netstat -ano | findstr :5173

# 结束占用进程（Windows）
taskkill /PID <进程ID> /F

# 或者更改端口
# 在.env文件中修改 PORT=3001
```

### 4. 数据库连接失败

**问题描述**: SQLite数据库无法创建或连接。

**解决方案**:
```bash
# 确保数据库目录存在
mkdir -p database

# 检查文件权限
chmod 755 database/

# 手动初始化数据库
node server/scripts/init-admin.js
```

### 5. 依赖安装失败

**问题描述**: npm install 过程中报错。

**解决方案**:
```bash
# 清理npm缓存
npm cache clean --force

# 删除node_modules重新安装
rm -rf node_modules package-lock.json
npm install

# 使用淘宝镜像（国内用户）
npm config set registry https://registry.npmmirror.com
```

### 6. 前端构建失败

**问题描述**: 运行 `npm run build` 失败。

**解决方案**:
```bash
# 运行构建测试
node test-frontend.js

# 手动构建
cd client
npm run build

# 如果仍然失败，检查具体错误信息
npm run build --verbose
```

### 7. JWT认证失败

**问题描述**: 登录后仍然提示未认证。

**解决方案**:
1. 检查环境变量中的JWT_SECRET
2. 清除浏览器localStorage
3. 确保后端服务器正在运行

```bash
# 重置JWT密钥
echo "JWT_SECRET=new-secret-key-$(date +%s)" >> .env

# 重启服务器
npm run dev
```

### 8. CORS跨域问题

**问题描述**: 前端无法访问后端API。

**解决方案**:
1. 确保后端CORS配置正确
2. 检查Vite代理配置
3. 确认后端服务器正在运行

### 9. 管理员账户问题

**问题描述**: 无法登录或忘记管理员密码。

**解决方案**:
```bash
# 修复密码哈希
node server/scripts/fix-admin-password.js

# 重新创建管理员账户
node server/scripts/init-admin.js

# 手动修改密码（需要了解数据库操作）
sqlite3 database/team.db
> UPDATE Members SET password_hash = '$2b$10$TYCVzLH6YN2tNVpUzQkFGOI/zsgje6BVj1oikqL8xT9MqFT.P5j2m' WHERE email_private = 'admin@team.com';
```

## 🔧 诊断步骤

### 基础检查清单

1. **Node.js版本**: 确保版本 >= 16
   ```bash
   node --version
   ```

2. **依赖安装**: 检查所有依赖是否正确安装
   ```bash
   npm list --depth=0
   cd client && npm list --depth=0
   ```

3. **端口状态**: 检查目标端口是否可用
   ```bash
   netstat -an | grep :3000
   netstat -an | grep :5173
   ```

4. **文件权限**: 确保关键文件有读写权限
   ```bash
   ls -la database/
   ls -la server/
   ls -la client/
   ```

5. **环境变量**: 检查.env文件配置
   ```bash
   cat .env
   ```

### 详细诊断

运行完整诊断脚本：
```bash
node test-system.js
```

这将测试：
- 服务器连接性
- API接口可用性
- 数据库访问
- 认证系统

## 🆘 获取帮助

如果以上解决方案都无法解决您的问题：

1. **查看日志**: 检查控制台输出的详细错误信息
2. **重启系统**: 有时简单的重启可以解决临时问题
3. **重新开始**: 删除所有文件后重新开始构建

```bash
# 完全重新开始
rm -rf node_modules client/node_modules client/dist database/*.db
npm install && cd client && npm install && cd .. && npm run dev
```

## 📞 技术支持信息

- **项目GitHub**: 提交Issue获取帮助
- **Node.js文档**: https://nodejs.org/docs/
- **Vue.js文档**: https://vuejs.org/guide/
- **SQLite文档**: https://www.sqlite.org/docs.html

---

💡 **提示**: 大多数问题都是由于依赖未正确安装或端口冲突导致的，请优先检查这些方面。